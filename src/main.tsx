import React from "react";
import ReactDOM from "react-dom/client";
import RocketInputController from "./";
import "./main.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RocketInputController />
  </React.StrictMode>,
);
